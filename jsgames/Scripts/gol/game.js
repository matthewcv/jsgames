(function() {

    window.gol = {};

    window.gol.Game = function(rows, cols, draw) {
        this.rows = rows;
        this.cols = cols;
        this.draw = draw;

        this.setRandomPattern = function(cellCount) {
            
        };
    };


})();